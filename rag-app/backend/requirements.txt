# Pinned for reproducible hackathon builds. chromadb is pinned to the same
# version as the chroma server image in docker-compose.yml.
fastapi==0.141.1
uvicorn[standard]==0.52.0
chromadb==1.3.0
sentence-transformers==5.6.1
openai==2.51.0
